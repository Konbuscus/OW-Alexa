'use strict';

// initialize express server for webhook functionality

// const app = require('../index').App;
const app = require('../index').Jovo;
const webhook = require('../index').Webhook;
const FilePersistence = require('../index').FilePersistence;

webhook.listen(3000, function() {
    console.log('Example server listening on port 3000!');
});


// listen for post requests
webhook.post('/webhook', function(req, res) {
    app.handleRequest(req, res, notifierAppHandlers);
    app.logIntent();
    app.setPersistence(new FilePersistence());
    // app.logResponse(false);
    // app.logRequest();
    app.execute();
});


let notifierAppHandlers = {
    'LAUNCH': function() {
        // app.tell('How you doin\'?');
        app.goTo('HelloWorldIntent');
    },
    'HelloWorldIntent': function() {
        // app.tell('How you doin\'?');
         app.audioPlayer().play('https://www.swetlow.de/silence.mp3',
        'silence');
        // app.audioPlayer().stop();
    },
    'AMAZON.ResumeIntent': function() {
        // DO STUFF
    },
    'AMAZON.PauseIntent': function() {
        app.audioPlayer().stop();
    },

    'AUDIOPLAYER': {
        'AudioPlayer.PlaybackStarted': function() {
            console.log('AudioPlayer.PlaybackStarted');
        },
        'AudioPlayer.PlaybackNearlyFinished': function() {
            console.log('AudioPlayer.PlaybackNearlyFinished');
            if (app.email) {
                console.log(app.email);
                // app.audioPlayer().play('https://www.swetlow.de/notification.mp3',
                //     'silence');
            } else {
                console.log("no email");
                // app.audioPlayer().play('https://www.swetlow.de/silence.mp3',
                //     'silence');
            }
            app.audioPlayer().stop();
        },
        'AudioPlayer.PlaybackStopped': function() {
            // console.log('AudioPlayer.PlaybackStopped');
            // app.audioPlayer().play('https://www.swetlow.de/notification.mp3',
            //     'silence');
        },
    },
};


let handlers = {
    'LAUNCH': function() {
       app.goTo('HelloWorldIntent');
    },
    'HelloWorldIntent': function() {
        app.audioPlayer()
            .play('https://feeds.soundcloud.com/stream/275202399-amazon-web-services-306355661-amazon-web-services.mp3',
         'token2');
    },
    'NameIntent': function() {
        app.dbLoad('offsetInMilliseconds', function(err, obj) {
            console.log('loaded', obj);
            app.tell('offset ' + obj);
        });
        // let offsetInMilliseconds = 100;
        // app.dbSave('offsetInMilliseconds', offsetInMilliseconds, function() {
        //     app.tell('offset saved' + offsetInMilliseconds);
        // });

    },
    'AMAZON.ResumeIntent': function() {
        app.dbLoad('offsetInMilliseconds', function(err, res) {
            app.audioPlayer()
                .setOffsetInMilliseconds(res)
                .play('https://feeds.soundcloud.com/stream/275202399-amazon-web-services-306355661-amazon-web-services.mp3', 'token2')
            ;
        });
    },
    'AMAZON.PauseIntent': function() {
        // app.emit('respond', app);

        app.dbSave('offsetInMilliseconds', app.audioPlayer().getOffsetInMilliseconds(), function() {
            app.audioPlayer().stop();
        });
    },

    'AUDIOPLAYER': {
        'AudioPlayer.PlaybackStarted': function() {
            console.log('AudioPlayer.PlaybackStarted');
        },
        'AudioPlayer.PlaybackNearlyFinished': function() {
            console.log('AudioPlayer.PlaybackNearlyFinished');
        },
        'AudioPlayer.PlaybackStopped': function() {
            console.log('AudioPlayer.PlaybackStopped');
        },
    },
};


let handlers2 = {
    'LAUNCH': function() {
        app.ask('Yo', 'Yo?');

        // app.tell('with card').withSimpleCard('title','content');

        // app.ask('Hey?','Bla')
        //     .toState('YesNoState')
        //     .addSessionAttribute('bla','blabla')
        //     .addSessionAttribute('blub','blubblub');

        // app.ask('What's your name?','Name?')
        //     .addSessionAttribute('foo','bar')
        //     .addSessionAttribute('hello','world');
            // .addSessionAttribute('foo','bar')
            // .addSessionAttribute('hello','world');


        // app.goTo('TestTellInput','tests');
        // app.goTo('TestTellSlot','tests', app.getSlotValue('name'));

        // app.tell('hey ho').addAssistantBasicCard('title', 'formattedText');
        //     app.ask('What's your name?', 'Your name please.');

        // app.response = 'bla';
        // app.goTo('HelloWorldIntent');
    },

    'YesNoState': {
        'YesIntent': function() {
            app.tell('You said yes in state.');
        },

        'NoIntent': function() {
            app.tell('You said no in state.');
        },
    },

    'YesIntent': function() {
        app.tell('You said yes.');
    },

    'NoIntent': function() {
        app.tell('You said no.');
    },


    'END1': function() {
        // clean up
        console.log(app.getEndReason());

        // app.tell('bye');
    },

    'HelloWorldIntent': function() {
        // app.tell('hey hey').addAssistantBasicCard('title', 'formattedText');
        app.ask('What\'s your name?', 'Name?')
            .addSessionAttribute('foo', 'bar')
            .addSessionAttribute('hello', 'world');
    },

    'PinActivate': function() {
    },

    'NameIntent': function() {
        app.tell('test123');
        // app.tell('with card').withSimpleCard('title','content');
        // app.goTo('TestTell','tests');
        // console.log(app.getSessionAttributes());

        // app.tell('with card').withSimpleCard('title','subtitle','content');

        // app.goTo('TestTellInput','tests', app.getInput('name'));


        // let speech = app.speechBuilder()
        //     .addText('hey')
        //     .addBreak('500ms')
        //     .addAudio('https://www.swetlow.de/meinruf_nitdeiner2.mp3')
        //     .addText('yoyoyo')
        //     .build();

        // app.tell('hey ' + app.getSessionAttribute('foo'));

        // app.ask('Hey ' + app.getSlotValue('name'), 'bla');


        // app.setAttribute('name', app.getSlotValue('name'));
        // app.goTo('HelpIntent','state');
        // app.getSocketIO().emit('event', { name: app.getSlotValue('name') });
        // app.tell('hey');
        // app.sayNothingEndSession();
    },

    'state': {
        'HelpIntent': function() {
            app.tell('test');
        },
    },

    'onboarding': {
        'NameIntent': function() {
            app.setAttribute('name', app.getSlotValue('name'));
            app.toState('onboarding').ask('Hey ' + app.getAttribute('name') + '. How old are you?', 'I didn\'t get your age. Please repeat');
            app.getSocketIO().emit('event', {name: app.getSlotValue('name')});
        },

        'AgeIntent': function() {
            app.setAttribute('age', app.getSlotValue('age'));
            app.toState('onboarding').ask('Where are you from?', 'I didnt\'t get that. Where are you from?');
        },
        'LocationIntent': function() {
            app.setAttribute('location', app.getSlotValue('location'));

            let name = app.getAttribute('name');
            let age = app.getAttribute('age');
            let location = app.getAttribute('location');

            app.tell('Hey ' + name + '! You are ' + age + ' years old. You are from ' + location);
        },
    },

    'tests': {

        'TestTell': function() {
            app.tell('test tell');
        },

        'TestTellInput': function(arg) {
            app.tell('test tell slot ' + arg);
        },

        'TestPlay': function() {
            app.play('https://www.swetlow.de/meinruf_nitdeiner2.mp3', 'Something bla');
        },
    },
};
